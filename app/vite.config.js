import { defineConfig } from 'vite';

// Deployed to GitHub Pages from the gtm-lab repo → served at /gtm-lab/.
// If this app is ever extracted to its own repo, change base to '/<repo-name>/'.
export default defineConfig({
    base: '/gtm-lab/',
});
